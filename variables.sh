#!/bin/bash
#Variables are partitions of memory in a computer program which store data; something which stores something.
#This script demonstrates the implementation of variables in Bash.
name='Zack' #This is how a variable is set. the variable "name" is declared and set with a string value "John".
echo "Hi $name" #To echo a variable, you must call it by prepending a dollar sign ($) to the variable name as done here.
